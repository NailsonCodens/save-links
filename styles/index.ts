import {createStitches} from '@stitches/react'

export const {
  config,
  styled,
  globalCss,
  keyframes,
  getCssText,
  css,
  createTheme,
  theme
} = createStitches({
  theme: {
    colors: {
      borderBottomMenu: '#000',
      gray900: '#fff',
      
    },    
  }  
})

